import { ButtonClicked2Directive } from './button-clicked-2.directive';

describe('ButtonClicked2Directive', () => {
  it('should create an instance', () => {
    const directive = new ButtonClicked2Directive();
    expect(directive).toBeTruthy();
  });
});
