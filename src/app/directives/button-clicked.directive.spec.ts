import { ButtonClickedDirective } from './button-clicked.directive';

describe('ButtonClickedDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonClickedDirective();
    expect(directive).toBeTruthy();
  });
});
