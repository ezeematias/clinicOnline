import { ButtonRadiusSensitiveDirective } from './button-radius-sensitive.directive';

describe('ButtonRadiusSensitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonRadiusSensitiveDirective();
    expect(directive).toBeTruthy();
  });
});
