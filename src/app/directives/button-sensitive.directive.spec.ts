import { ButtonSensitiveDirective } from './button-sensitive.directive';

describe('ButtonSensitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSensitiveDirective();
    expect(directive).toBeTruthy();
  });
});
