import { Captcha2Directive } from './captcha2.directive';

describe('Captcha2Directive', () => {
  it('should create an instance', () => {
    const directive = new Captcha2Directive();
    expect(directive).toBeTruthy();
  });
});
