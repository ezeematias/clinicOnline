import { ImgChangeDirective } from './img-change.directive';

describe('ImgChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgChangeDirective();
    expect(directive).toBeTruthy();
  });
});
