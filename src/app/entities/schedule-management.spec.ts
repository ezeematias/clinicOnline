import { ScheduleManagement } from './schedule-management';

describe('ScheduleManagement', () => {
  it('should create an instance', () => {
    expect(new ScheduleManagement()).toBeTruthy();
  });
});
