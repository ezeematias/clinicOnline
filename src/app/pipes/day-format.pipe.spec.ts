import { DayFormatPipe } from './day-format.pipe';

describe('DayFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DayFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
