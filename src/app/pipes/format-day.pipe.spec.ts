import { FormatDayPipe } from './format-day.pipe';

describe('FormatDayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDayPipe();
    expect(pipe).toBeTruthy();
  });
});
