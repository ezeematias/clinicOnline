import { ListTostringPipe } from './list-tostring.pipe';

describe('ListTostringPipe', () => {
  it('create an instance', () => {
    const pipe = new ListTostringPipe();
    expect(pipe).toBeTruthy();
  });
});
