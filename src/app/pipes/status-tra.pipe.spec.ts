import { StatusTraPipe } from './status-tra.pipe';

describe('StatusTraPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTraPipe();
    expect(pipe).toBeTruthy();
  });
});
