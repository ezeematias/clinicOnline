import { SummaryDinamicPipe } from './summary-dinamic.pipe';

describe('SummaryDinamicPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryDinamicPipe();
    expect(pipe).toBeTruthy();
  });
});
