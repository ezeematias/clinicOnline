import { SummaryFilterSmallPipe } from './summary-filter-small.pipe';

describe('SummaryFilterSmallPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryFilterSmallPipe();
    expect(pipe).toBeTruthy();
  });
});
