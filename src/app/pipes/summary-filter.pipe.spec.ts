import { SummaryFilterPipe } from './summary-filter.pipe';

describe('SummaryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
