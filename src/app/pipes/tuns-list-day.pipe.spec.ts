import { TunsListDayPipe } from './tuns-list-day.pipe';

describe('TunsListDayPipe', () => {
  it('create an instance', () => {
    const pipe = new TunsListDayPipe();
    expect(pipe).toBeTruthy();
  });
});
