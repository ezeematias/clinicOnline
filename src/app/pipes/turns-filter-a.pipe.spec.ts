import { TurnsFilterAPipe } from './turns-filter-a.pipe';

describe('TurnsFilterAPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnsFilterAPipe();
    expect(pipe).toBeTruthy();
  });
});
