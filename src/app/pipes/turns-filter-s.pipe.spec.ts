import { TurnsFilterSPipe } from './turns-filter-s.pipe';

describe('TurnsFilterSPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnsFilterSPipe();
    expect(pipe).toBeTruthy();
  });
});
