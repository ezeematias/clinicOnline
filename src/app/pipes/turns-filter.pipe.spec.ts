import { TurnsFilterPipe } from './turns-filter.pipe';

describe('TurnsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
